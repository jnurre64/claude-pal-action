# Slack bot

Read `CLAUDE.md` and `README.md` in this directory for architecture, setup, and
test instructions. Shared bot functionality lives in `../shared/`; inspect it
before duplicating behavior. Run the affected bot and shared Python tests when
changing their behavior.
